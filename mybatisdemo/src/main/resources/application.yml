server:
  port: 8889
logging:
  path: logs
  file: mylog.log
spring:
  application:
    name: lunimeisSpringApplication
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/mydata?serverTimezone=CTT&useUnicode=true&characterEncoding=utf-8&allowMultiQueries=true
    username: root
    password: root
mybatis:
  type-aliases-package: com.tester.model
  mapper-locations: classpath:mapper/*.xml

